# Clementine Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================
# PUBLIC VARIABLES (Client SDK - NEXT_PUBLIC_*)
# ============================================
# These are exposed to the browser and used by Firebase Client SDK

# Base URL for the application (used for join URLs and share links)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Firebase Client SDK Configuration
# Get these from Firebase Console > Project Settings > General > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# ============================================
# SERVER-ONLY VARIABLES (Admin SDK)
# ============================================
# These are only accessible on the server (Next.js Server Actions)

# Firebase Admin SDK Service Account
# Get these from Firebase Console > Project Settings > Service Accounts > Generate new private key
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Note: The private key should be the full key from the service account JSON
# If copy-pasting, ensure newlines are preserved or use \\n
FIREBASE_PRIVATE_KEY=

# Firebase Storage bucket (same as public one, but used by Admin SDK)
FIREBASE_STORAGE_BUCKET=

# ============================================
# AI PROVIDER CONFIGURATION
# ============================================
# Select AI provider: 'google-ai' | 'n8n' | 'mock'
# Defaults to 'mock' if not set or if required credentials are missing
AI_PROVIDER=mock

# Google AI (when AI_PROVIDER=google-ai)
# Get API key from: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=

# n8n Webhooks (when AI_PROVIDER=n8n)
# Base URL for your n8n instance webhook endpoints
N8N_WEBHOOK_BASE_URL=
# Optional: Authentication token for n8n webhooks
N8N_WEBHOOK_AUTH_TOKEN=

# ============================================
# DEPRECATED (remove after refactor)
# ============================================
# NANO_BANANA_API_KEY=
# NANO_BANANA_BG_SWAP_ENDPOINT=
# NANO_BANANA_DEEPFAKE_ENDPOINT=


ADMIN_SECRET=