{
  "name": "scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate:ai-presets": "node --import=tsx migrate-experiences-to-ai-presets.ts",
    "migrate:projects": "node --import=tsx migrate-events-to-projects.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
