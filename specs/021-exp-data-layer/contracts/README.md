# Contracts

Not applicable for this feature. Experience data layer uses Firebase client SDK directly with TanStack Query hooks. No REST API endpoints are created.

See `data-model.md` for entity schemas and `quickstart.md` for hook usage examples.
