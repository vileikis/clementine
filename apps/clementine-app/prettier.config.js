// @ts-check

// Note: This config is inherited from /prettier.config.js at the monorepo root.
// Only keep this file if you need package-specific overrides when opening this folder directly.

/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
}

export default config
