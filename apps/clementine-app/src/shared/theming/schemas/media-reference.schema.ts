/**
 * MediaReference Schema (App Re-export)
 *
 * Re-exports MediaReference schema from @clementine/shared for app usage.
 * Single source of truth is in packages/shared/src/schemas/theme/
 */

export { mediaReferenceSchema, type MediaReference } from '@clementine/shared'
