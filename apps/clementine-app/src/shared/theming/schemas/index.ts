// Theming schemas barrel export
export {
  COLOR_REGEX,
  themeTextSchema,
  themeButtonSchema,
  themeBackgroundSchema,
  themeSchema,
  THEME_DEFAULTS,
} from './theme.schemas'

export type {
  Theme,
  ThemeText,
  ThemeButton,
  ThemeBackground,
} from './theme.schemas'
