/**
 * Tests for useLibraryPicker hook
 */

import { describe, expect, it } from 'vitest'

describe('useLibraryPicker', () => {
  it.todo('should provide open method to trigger file picker')
  it.todo('should validate file type (image/jpeg, image/png)')
  it.todo('should validate file size (max 10MB)')
  it.todo('should call onPhoto with valid file')
})
