/**
 * Project Config Designer Store
 *
 * Zustand store for tracking save state in the project config designer.
 * Created using the shared editor store factory.
 */
import { createEditorStore } from '@/shared/editor-status'

export const useProjectConfigDesignerStore = createEditorStore()
