/**
 * Event Designer Store
 *
 * Zustand store for tracking save state in the event designer.
 * Created using the shared editor store factory.
 */
import { createEditorStore } from '@/shared/editor-status'

export const useEventDesignerStore = createEditorStore()
