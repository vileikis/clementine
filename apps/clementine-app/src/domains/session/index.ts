/**
 * Session Domain - Main Barrel Export
 *
 * Entry point for importing from @/domains/session
 *
 * Usage:
 * ```typescript
 * import { Session, sessionSchema, SessionMode } from '@/domains/session'
 * ```
 */

export * from './shared'
