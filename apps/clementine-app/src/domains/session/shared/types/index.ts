/**
 * Session Domain - Types Barrel Export
 *
 * Exports all TypeScript types and interfaces for the session domain.
 * Note: Base session types (Session, SessionMode, etc.) are
 * exported from ../schemas to avoid duplicate exports.
 */

// Session API types
export * from './session-api.types'
