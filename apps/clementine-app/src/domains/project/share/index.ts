/**
 * Barrel export for Project Share Dialog domain
 * Feature: 011-project-share-dialog
 *
 * Only exports the ShareDialog component (public API)
 * Internal components, hooks, and utilities are not exported
 */

export { ShareDialog } from './components/ShareDialog'
export type { ShareDialogProps } from './types'
